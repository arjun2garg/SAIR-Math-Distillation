# Old results — single-cheatsheet evaluation

Archive of the previous iteration of this repository. The earlier work
evaluated a single cheatsheet (`bank_lookup_v5.txt` and later `v10b`) at a time
and reported per-rule coverage on the full ETP matrix.

Contents:

| file | what it is |
|------|------------|
| `PREVIOUS_README.md` | the old top-level README (focused on `bank_lookup_v5.txt` coverage) |
| `SAIR_HF_EVAL_FINDINGS.md` | findings doc from running the v10b cheatsheet on the SAIR HuggingFace problem set |
| `v10b_evaluation_extra_hard_rule_breakdown.json` | per-problem results from the old v10b run on `evaluation_extra_hard` |
| `v10b_hard3_rule_breakdown.json` | per-problem results from the old v10b run on `hard3` |

The newer multi-cheatsheet generalization study lives at the top level of the
repo — see `README.md` and `eval_programmatic/`.
